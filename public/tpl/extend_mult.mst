<div class='col margin-bottom padding-tiny relative {{#form.cols}}s{{form.cols}} {{/form.cols}}{{#form.colm}}m{{form.colm}} {{/form.colm}}{{#form.coll}}l{{form.coll}} {{/form.coll}}'>
    <div class="col parent-input parent-relation relative radius border-bottom {{form.class}}"
         {{#form.style}}style="{{form.style}}"{{/form.style}}>

        <div class="col padding-small padding-8" style="margin-bottom: 0!important;">
            <div class="left color-text-grey padding-4 upper relative">
                {{nome}}
            </div>
            <button class="addExtend no-select radius right opacity hover-opacity-off color-gray hover-shadow margin-0 right"
                    style="height: 35px;"
                    onclick="addExtendMult('{{formIdentificador}}', '{{relation}}', '{{column}}', '{{id}}')" rel="{{id}}"
                {{#disabled}} disabled='disabled' {{/disabled}} >
                <i class="material-icons left transition-easy">add</i>
            </button>
        </div>

        <div class="extend_list col padding-small transition-slow" style="height: 0;overflow: hidden"></div>
        <div class="extend_list_register col transition-slow" rel="{{formIdentificador}}" style="overflow: hidden">
            {{#value}}
                <div class="col padding-small padding-8 transition-slow extend_register radius" rel="{{id}}" style="height:46px;overflow: hidden">
                    <div class="col">
                        <div class="col right" style="width: 46px">
                            <button onclick="deleteExtendMult('{{column}}', '{{id}}')" rel="{{id}}" class="right btn-small radius color-gray-light hover-theme opacity hover-opacity-off btn-del-extend-register transition-slow"
                                    data-column="{{column}}" data-id="{{id}}" style="overflow: hidden;margin-left: 5px; width: 38px" >
                                <i class="material-icons left font-large">delete</i>
                            </button>
                        </div>
                        <div class="rest pointer no-select" onclick="edtiExtendMult('{{relation}}', '{{column}}', '{{id}}')">
                            <button class="left btn-small radius theme hover-theme opacity hover-opacity-off btn-edit-extend-register transition-slow" rel="{{id}}" style="margin-top:1px">
                                <i class="material-icons left font-large transition-slow">edit</i>
                            </button>
                            <div class="left font-medium padding-4 padding-left extend_register_title" rel="{{id}}">{{{tituloExtend}}}</div>
                        </div>
                    </div>
                    <div class="col extend_register_edit transition-slow" style="height: 0;overflow: hidden"></div>
                </div>
            {{/value}}
        </div>
    </div>
</div>

<style>
    .extend_register:nth-child(odd)  {
        background: #fbfbfb;
        border:solid 1px #eee;
    }
</style>