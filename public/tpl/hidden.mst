<input
    type="hidden"
    data-format="{{format}}"
    data-column="{{column}}"
    data-parent="{{parent}}"
    class="formCrudInput"
    id="{{column}}"
    rel="{{formIdentificador}}"
    {{#value}}value="{{value}}"{{/value}}
/>