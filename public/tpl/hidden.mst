<div class='col relative'>
    <input
            type="hidden"
            data-format="{{format}}"
            class="formCrudInput"
            id="{{column}}"
            data-column="{{column}}"
            data-parent="{{parent}}"
            rel="{{formIdentificador}}"
    />
</div>